# =============================================================================
# AUTHENTICATION CONFIGURATION (NextAuth.js)
# =============================================================================

# Required: Secret used to encrypt cookies and tokens
# Generate using: node scripts/generate-auth-secret.js
# or: npx auth secret
AUTH_SECRET="your-generated-secret-here"

# Required: Discord OAuth Provider credentials
# Get these from: https://discord.com/developers/applications
# 1. Create a new application at Discord Developer Portal
# 2. Go to OAuth2 section and add redirect URL: http://localhost:3000/api/auth/callback/discord
# 3. Copy the Client ID and Client Secret
AUTH_DISCORD_ID="your-discord-client-id"
AUTH_DISCORD_SECRET="your-discord-client-secret"

# =============================================================================
# EMAIL CONFIGURATION (EmailJS)
# =============================================================================

# Required: EmailJS credentials for contact form
# Get these from: https://dashboard.emailjs.com
# 1. Create an account at EmailJS
# 2. Add your email service (Gmail, Outlook, etc.)
# 3. Create an email template
# 4. Get your Service ID from the Email Services page
# 5. Get your Template ID from the Email Templates page
# 6. Get your Public Key from Account > API Keys

# Note: These must be prefixed with NEXT_PUBLIC_ to be accessible in the browser
NEXT_PUBLIC_EMAILJS_SERVICE_ID="your-emailjs-service-id"
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID="your-emailjs-template-id"
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY="your-emailjs-public-key"
